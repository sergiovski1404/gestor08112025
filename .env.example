## Copie este arquivo para `.env.local` e preencha com valores reais
## Onde obter: Supabase → Settings → Configuration → API Keys

# URL do projeto (formato: https://<project-ref>.supabase.co)
VITE_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co

# Chave pública (anon) — token JWT que normalmente começa com "ey..."
VITE_SUPABASE_ANON_KEY=YOUR-ANON-PUBLIC-KEY

# Opcional: chave da API Google para Gemini
VITE_GOOGLE_API_KEY=YOUR-GEMINI-KEY
